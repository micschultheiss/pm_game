# Python bytecode / caches
__pycache__/
*.py[cod]

# Test / coverage artifacts
.coverage
htmlcov/
.pytest_cache/

# macOS
.DS_Store

# Claude Code — machine-local settings (keep launch.json shareable)
.claude/settings.local.json
# Linear board watcher — per-checkout transition snapshot (see scripts/linear_watch.py)
.claude/linear_watch_state.json
